module scene;

public struct Sphere{
    public float4 postion; // xyz: positon | w: radius
    public float4 colour;  // xyz: rgb | w: empty
    public float4 matrialProperties; // x: roughness | y: metalness | z: emission | w: unused
}

